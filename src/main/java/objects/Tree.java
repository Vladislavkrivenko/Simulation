package objects;

import animals.Entity;
import createMap.Coordinates;

public class Tree extends Entity {

	public Tree(Coordinates coordinates, ColorObject color) {
		super(coordinates, color);
	}
	// private static final Tree tree = new Tree();

}
