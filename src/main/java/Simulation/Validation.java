package Simulation;

import animals.Creature;
import animals.Entity;
import animals.Herbivore;
import animals.Predator;

/*
 * проверка на диапазон атаки проверка что заець не ест(зайца,деревья,горы,волк)
 * волк не должен есть горы,деревья
 *  выход животных за карту
 *  входные даные
 */
/*public class Validation extends Creature {
	public Validation(String typeOfAnimal, int animalHealthLevel, int animalAttackPower, int animalSpeed) {
		super(typeOfAnimal, animalHealthLevel, animalAttackPower, animalSpeed);

	}

	/*
	 * Entity entity; Creature animal; Herbivore rabbit; Predator wolf;
	 * 
	 * private boolean isMakeAttackWolf(Herbivore rabbit, Predator wolf, Entity
	 * entity) { if (wolf.equals(rabbit)) { return true; } else if
	 * (wolf.equals(TREE)) { return false; } else if (wolf.equals(ROCK)) { return
	 * false; } else if (wolf.equals(GRASS)) { return false; } else { return false;
	 * } }
	 * 
	 * private boolean isMakeAttackRabbit(Herbivore rabbit, Predator wolf, Entity
	 * entity) { if (rabbit.equals(rabbit)) { return false; } else if
	 * (rabbit.equals(TREE)) { return false; } else if (rabbit.equals(ROCK)) {
	 * return false; } else if (rabbit.equals(GRASS)) { return true; } else
	 * if(rabbit.equals(wolf)){ return false; } return false; }
	 * 
	 * @Override public void makeMove() { makeAttack();
	 * 
	 * }
	 * 
	 * @Override public void IMeasureTheAttackRadius() {
	 * 
	 * 
	 * }
	 * 
	 * @Override public void createAnimal() {
	 * 
	 * 
	 * }
	 * 
	 * @Override public void makeAttack() { isMakeAttackWolf(rabbit, wolf, entity);
	 * isMakeAttackRabbit(rabbit, wolf, entity);
	 * 
	  }
	 
}*/
